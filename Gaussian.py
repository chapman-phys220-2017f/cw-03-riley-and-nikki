#!/usr/bin/env python3

import math
def g(x):
    return(1/math.sqrt(2*math.pi))*math.exp(-x**2/2)    

def interval(f,a,b,dx):
    l=[]
    acc = |f(a),f(a+(N-1)dx),f(b)|
    b = a+N*dx
def integrate(i,dx):
